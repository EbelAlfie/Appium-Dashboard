import { useEffect, useState } from "react"

export const DeviceList = () => {
    const [devices, setDevices] = useState()

    useEffect(() => {

    }, [])

    return (
        <>
            <div>

            </div>
        </>
    )
}